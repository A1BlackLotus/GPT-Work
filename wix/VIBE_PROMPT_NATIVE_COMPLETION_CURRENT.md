# Behavioral Bridge — Vibe Prompt — Native Completion Pass — CURRENT

## CURRENT EXECUTION UPDATE — independently rechecked 2026-09-13 UTC

Execute this bounded native repair now. This update and the full specification below are one prompt. Complete source work and available verification; do not merely return another plan. DO NOT PUBLISH.

Independent recheck found:
1. Native Home still visibly uses Ryan's portrait; public Home still identifies the lighthouse. The approved bridge remains required.
2. The currently visible Vibe report still claims Results COMPLETE with the testimonials CMS and 5%-opacity image. A fresh Wix query confirms that CMS contains exactly Sarah M., David L., and Emily R. These are not accepted Results content.
3. Public Results desktop screenshot confirms the accepted visible split hero. Its full page tree contains the seven accepted summaries. Reproduce that natively without runtime observer/hide logic.
4. Public Results still uses the shortened EF sequence. Native final sequence must be CAPTURE → PLAN → PRIORITIZE → START → SUSTAIN → MONITOR → FINISH.
5. Public Homework/Motivation trees contain their correct cover and one respective Try This/Key Idea heading, but not their required body infographic. Inspect actual fetched Ricos nodes and source to locate the exact failure.
6. Your visible prior report claims the four article-specific components are injected and cites CSS classes as responsive evidence. Those statements do not satisfy final architecture or visual acceptance.
7. No exact executed lint/typecheck/build output was present in that report.

First inspect current code; preserve any newer correct work. Known source leads from your report:
- src/components/pages/PostPage.tsx
- src/components/pages/ResultsPage.tsx
- src/components/pages/HomePage.tsx
- src/components/Router.tsx
- src/lib/resourceArticles.ts
- BaseCrudService and the actual Ricos renderer/media helper
Follow real imports. Do not assume resourceArticles owns live rich content.

Implementation precision:
- Use actual IMAGE node data; preserve alt, valid dimensions/aspect ratio, alignment and captions.
- Reuse verified URL resolution. Do not prefix an existing URL as a media ID.
- Do not use global image-ID deduplication that removes legitimate body images.
- Gracefully handle malformed nodes without crashing the page.
- Remove duplicate slug injections only after generic rendering works; remove unused imports only after checking other consumers.
- Keep Blog copy and nodes intact.
- Results summaries are paraphrases, not verbatim quotes.
- VIEW VERIFIED REVIEWS should retain the accepted public Wyzant destination:
  https://www.wyzant.com/Tutors/MA/Brookline/10206436/
  Do not substitute #reviews for this external verification action.
- Keep Superprof source accurate:
  https://www.superprof.com/elite-english-reading-and-writing-tutor-improving-literacy-essays-and-academic-skills-perfect-wyzant-star-reviews-275.html
  Numbers in this URL are not visible proof claims and must not be blindly replaced.
- Preserve the accepted referral benefit and its separation from reviews. Do not invent additional terms.
- Home: preserve current headline/copy/CTAs and page structure; replace only the hero visual. Retain important embedded image details.
- Preserve runtime protection; if it masks native preview, report the verification limitation rather than disabling it.

Actual responsive checks:
Use supported native preview controls at desktop, tablet, mobile and narrow mobile. Suggested widths: 1440, 1024/768, 390 and 320 CSS px. Record actual dimensions.
Inspect Results/Home at all available sizes; both priority articles at desktop/mobile; one ordinary article for shared-reader regression.
Count body headings, not duplicate text appearances in TOC/accessibility descendants, when checking callouts.
If imagery is too detailed for mobile reading, preserve natural ratio and provide an accessible full-size image link using existing conventions.

Validation:
Run package.json's actual configured lint, typecheck and production build if command execution exists. Record exact command, execution result/exit status and relevant output. Do not equate source review, READY badge or working preview with a build pass.
Use NOT RUN or NOT INSPECTED when unavailable. Separate old failures from introduced errors. Repair introduced errors before stopping.

Finish priorities below before unrelated SEO, consultation transport, domain, analytics or redesign work. Those remain required overall completion gates, but do not mix them into this repair batch.
No production form submission; no Blog publication; no patch retirement; no CMS mutation.

Return a delta-based report: changed files and source owners, exact root cause/fix, image and callout counts, accepted review parity, hero evidence, actual viewport checks, command output, and remaining blockers. Include current project version if exposed. Do not claim full website completion.

---

Work in the current native Wix Vibe source for Behavioral Bridge site `e6f68a23-cc00-421a-985e-515963cbe5f0`.

This is a SOURCE-REPAIR + NATIVE-PARITY pass.

**DO NOT PUBLISH.**

The public site is still protected by working runtime compatibility layers. Do not disable, replace, or retire them in this run.

## Read the current state before editing

The last Vibe report is not accepted as proof. Several claims were contradicted by direct Wix/public checks.

### Confirmed current Home state
- Public live Home still uses the lighthouse hero.
- Native Vibe Home preview currently uses Ryan's portrait as the hero.
- The final approved replacement is neither of those.
- Use the already-uploaded Media Manager asset:
  - `BB_Home_Hero_Bridge-Clarity-Guidance-Structure-Independence_Editorial_v1`
  - media ID `445f86_55d560892aba4714b627a1e87fad1ee8~mv2.png`

### Confirmed current Results problem
Your previous report said native Results review cards pull from the `testimonials` CMS collection and that Results is complete.

Direct Wix CMS inspection proves `testimonials` currently contains only 3 old placeholder records:
- Sarah M. — SAT Tutoring — generic `over 200 points` quote
- David L. — EF Coaching — generic organization quote
- Emily R. — College Application Assistance — generic application/essay quote

These are NOT the accepted public Results reviews.

Therefore native Results is NOT at parity.

Do not delete or mutate that CMS collection in this pass. It may have historical dependencies.

### Confirmed accepted Results review set
Use one dedicated native review-summary owner for Results. If no verified correct native owner already exists, create a small source module/constant used only by Results rather than relying on the placeholder CMS collection.

Use exactly this accepted set:

1. Maria — WYZANT · 22 lessons — SAT Parent
   `Structured, strategic SAT preparation, organized lessons, stronger confidence, and visible score progress after trying several other tutors.`

2. Reeta — WYZANT · 10 lessons — SAT Parent
   `Targeted diagnosis of specific weaknesses, tailored instruction, and noticeable improvement on practice tests.`

3. Brian — WYZANT · 36 lessons — Parent
   `Patient, focused support for an autistic and legally blind student, practical SAT strategies, useful parent tools, and growing confidence.`

4. Leon — WYZANT · 1 lesson — Parent
   `Exceptional preparation, professionalism, and meaningful work between sessions.`

5. Terri — WYZANT · 30 lessons — Parent
   `Thoroughness, strong student rapport, and an approach designed to build academic habits that stick.`

6. Brak — WYZANT · 12 lessons — Parent
   `Patience, calm support, and individualized SAT preparation for an autistic high-school student.`

7. Bryan — SUPERPROF · PUBLIC RECOMMENDATION — Parent · Superprof
   `Patient, encouraging support that meets the student where he is while steadily building confidence and engagement.`

Preserve the disclosure:
`Cards paraphrase public Wyzant reviews and one Superprof recommendation. Individual results vary; no score change is guaranteed.`

### Confirmed Results hero mismatch
Your previous report said the approved Results image was used as a 5%-opacity background.

That is NOT the accepted public visual reference.

The accepted public Results hero is a clear split/two-column dark editorial hero:
- left: copy/actions on dark background
- right: the approved Results image visibly displayed, not washed out at 5%

Approved Results hero media:
`445f86_980f4b71904345e0a8b11fca935361b8~mv2.png`

Rebuild native Results to match that accepted visual hierarchy.

## PRIORITY 1 — FIX THE SHARED BLOG RENDERER CORRECTLY

Inspect the actual current `src/components/pages/PostPage.tsx` and every helper/component it uses for Wix Blog Ricos rendering.

Your previous report says the file currently injects these article-specific components around the content area:
- `HomeworkProcessVisual`
- `HomeworkCallout`
- `MotivationSystemVisual`
- `MotivationCallout`

If those are still present, they are transitional/wrong for the current data state.

The two published Blog posts ALREADY contain their final external Ricos IMAGE nodes and their callout text. Do not insert them again.

### Homework published data already contains
Route:
`/post/homework-initiation-executive-function`

IMAGE media ID:
`445f86_a1f958393d6a47258ab6773c31e61639~mv2.jpg`

Alt text:
`Homework Start Sequence: identify task, first visible action, start cue, reduce distraction, short work block, review next step`

The Blog body also already contains a `Try This` heading and its action paragraph.

### Motivation published data already contains
Route:
`/post/why-motivation-is-wrong-target-executive-function`

IMAGE media ID:
`445f86_95ec120daff34701addcf9a9e68209a1~mv2.jpg`

Alt text:
`Build Action, Not Motivation: visible cue, concrete next action, protected start, short feedback loop, recovery rule`

The Blog body also already contains a `Key Idea` heading and its paragraph.

### Required PostPage architecture
1. Inspect how all Ricos nodes are currently mapped.
2. Add first-class generic support for `node.type === 'IMAGE'` if it is absent or broken.
3. Use the actual Ricos shape from the current fetched post data. Prefer `imageData.image.src.id` when present.
4. Reuse an existing Wix-media URL resolver/helper if the source already has one.
5. Do not hardcode only the two image IDs into `PostPage.tsx`.
6. Preserve `imageData.altText`.
7. Preserve width/height/aspect ratio where present.
8. Render responsively: max width 100%, height auto, no overflow, sensible article-column width.
9. Preserve alignment/caption data where supported.
10. Do not duplicate the hero image.
11. Preserve paragraphs, headings, lists, links, TOC, metadata, related reading, CTA, loading/error/not-found states.
12. Do not revive the retired custom Article Reader or Resources Router.

After the generic Ricos IMAGE nodes render correctly, remove/retire the slug-specific `HomeworkProcessVisual`, `HomeworkCallout`, `MotivationSystemVisual`, and `MotivationCallout` injections IF they are still present, because the Blog body now owns those visuals/text.

Do not leave duplicate visuals or duplicate callout copy.

If you want special callout styling, style the already-existing Blog `Try This` / `Key Idea` content through reusable article rendering logic. Do not inject duplicate article-specific prose.

## PRIORITY 2 — CORRECT NATIVE RESULTS FOR REAL PARITY

Inspect `src/components/pages/ResultsPage.tsx`, its data imports, route owner, and related components.

Do not trust the previous completion report. Verify the source.

### Hero
Use:
- eyebrow `REAL REVIEWS. SPECIFIC WORK.`
- H1 `Strong Students Don’t Need More Noise.`
- lead `They need to know what is actually breaking down.`
- supporting paragraph about finding the failure point for SAT/EF
- Book a Consultation action
- View Verified Reviews action
- visible approved Results image on the right in a split editorial hero

Do NOT use the image as a nearly invisible 5% background.

### Proof strip
Only:
- `5.0` — Wyzant rating
- `75+` — 5-star reviews
- `350+` — Wyzant + Superprof hours
- `Nearly 14` — Years experience
- `SAT + EF` — Two core services
- `Online` — Remote nationwide

Never introduce 60+, 240+, 300+, 12 years, 12+, 13 years, 75++, 350++, or Wyzant-only wording for the combined hours.

### Reviews
Do not use the current placeholder `testimonials` CMS data for this section.

Use the accepted seven review summaries above from one dedicated native owner.

Keep Wyzant and Superprof labels accurate.

### Accepted Results sections
Preserve/reproduce:
- `PUBLIC REVIEWS & RECOMMENDATIONS`
- `What Families Actually Mention`
- `What I Actually Measure`
- SAT Performance
- Executive Function
- Academic Support
- `CLASSIFY → ACTIVATE → PREDICT → PROVE`
- `CAPTURE → PLAN → PRIORITIZE → START → SUSTAIN → MONITOR → FINISH`
- `HOW I WORK`
- diagnosis before prescription
- accuracy before speed where appropriate
- practice tied to observed errors
- specific fix rules rather than vague advice
- useful parent communication
- independence as the endpoint
- referral section
- final consultation/email CTA

Do not tie referral benefit to leaving a review.

## PRIORITY 3 — INSTALL THE APPROVED HOME HERO

Inspect the actual native Home page/component that owns the hero image.

Current state:
- public = lighthouse
- native preview = Ryan portrait
- desired final native hero = approved external bridge image

Use Media Manager asset:
`BB_Home_Hero_Bridge-Clarity-Guidance-Structure-Independence_Editorial_v1`

Media ID:
`445f86_55d560892aba4714b627a1e87fad1ee8~mv2.png`

Do not regenerate it.
Do not delete the lighthouse or portrait assets.
Do not redesign the rest of Home.

Preserve current Home copy and CTA structure.

Integrate the bridge hero with deliberate responsive crop/fit on desktop/tablet/mobile.
Avoid extreme `cover` cropping.
No horizontal overflow.
No unnecessary animation.

Suggested alt intent:
`Behavioral Bridge pathway representing clarity, guidance, structure, and independence.`

## PRIORITY 4 — REAL VALIDATION, NOT A COMPLETION CLAIM

The prior report said `Build Status: READY`, but did not provide actual command output.

That is NOT accepted as validation.

Inspect `package.json` / project configuration and run the actual configured validation commands that are available, such as:
- ESLint
- TypeScript/typecheck
- production build

Report the exact command names and results.

If the Vibe environment cannot execute one or more commands, explicitly write `NOT RUN — environment does not expose command execution` rather than claiming PASS.

Fix errors introduced by this pass.
Do not broaden into unrelated refactoring.

## PRIORITY 5 — NATIVE PREVIEW ACCEPTANCE

Actually preview, not just inspect class names.

Check:
- Results desktop
- Results tablet
- Results mobile
- Results narrow mobile
- Homework desktop/mobile
- Motivation desktop/mobile
- Home desktop/tablet/mobile
- one ordinary untouched article

Verify:
- correct Results review names/source labels
- no Sarah M./David L./Emily R. placeholder review cards on Results
- Results hero has a visible right-side image
- no duplicate native/runtime content in preview
- Homework external infographic visibly renders in-body
- Motivation external infographic visibly renders in-body
- no duplicate Homework/Motivation visual or callout
- article infographic text remains readable
- related-reading cards remain intact
- CTA remains intact
- Home uses the approved bridge hero, not lighthouse or portrait
- no horizontal overflow
- no clipped text
- no broken image crop

## DO NOT TOUCH

Do not publish.
Do not disable Results runtime protection.
Do not disable consultation safety/fallback.
Do not retire compatibility code.
Do not mutate/delete the placeholder testimonials CMS collection.
Do not redesign SAT, EF, About, Resources globally, header, or footer.
Do not regenerate approved imagery.
Do not republish/reinsert the two article images or callout text — they are already published.
Do not publish the flagship ADHD article.
Do not run a real consultation submission.
Do not change domain, DNS, billing, plan, or Google/Search Console.
Do not delete Wix sites.

## COMPLETION REPORT FOR ASTRA

Return concrete evidence only:

1. Files inspected.
2. Files changed.
3. Exact PostPage IMAGE-rendering root cause.
4. Exact generic Ricos IMAGE implementation.
5. Whether slug-specific Homework/Motivation injected components were found and removed/retired.
6. Homework external image preview result.
7. Motivation external image preview result.
8. Whether each callout appears exactly once.
9. Ordinary-article regression result.
10. Exact native Results review data owner.
11. Confirmation that Sarah M./David L./Emily R. are NOT displayed on native Results.
12. Confirmation that all seven accepted review summaries appear correctly.
13. Results hero preview description and whether the approved image is visibly presented on the right.
14. Home hero media owner and confirmation that the approved bridge image is now used.
15. Exact desktop/tablet/mobile previews actually checked.
16. Exact ESLint command/result.
17. Exact TypeScript/typecheck command/result.
18. Exact production-build command/result.
19. Any validation that could not be run.
20. Remaining blockers before controlled publish.

**STOP BEFORE PUBLISHING.**

Desired end state:

`generic Ricos article IMAGE rendering correct + no duplicate article-specific injections + native Results using the accepted seven-review set and accepted split hero + approved bridge hero on Home + real available validation performed + responsive native previews checked + protected public runtime layers untouched + site still unpublished.`
