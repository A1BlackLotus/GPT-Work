# Behavioral Bridge — Post-Astra Website Resume — 2026-09-12

Use with:

- `wix/WEBSITE_EXECUTION_CHECKPOINT_CURRENT.md`
- `wix/WEBSITE_EXECUTION_PROTOCOL_CURRENT.md`

The checkpoint is authoritative for current state.

## Resume command

Ryan may say only `NEXT`, `NEXT STEP`, `GO`, or `CONTINUE`.

Interpret that as authorization to execute the highest-priority unfinished coherent unit from the CURRENT checkpoint, verify it, update the checkpoint, and stop only at a natural verification boundary or genuine owner-only blocker.

Do not ask Ryan to restate the project.

## Current release safety rule

**DO NOT PUBLISH THE CURRENT NATIVE VIBE DRAFT.**

Public `/results` is accepted/correct while native/editor Results is reported older. Public Results is materially supplied/protected by Results Showcase rev16 and related transitional runtime code. Native parity must be proven before controlled publication.

## Current protected runtime

Preserve:

- Results Showcase rev16 enabled
- Results Visual Repair rev3 enabled
- Proof Metrics rev8 enabled
- Consultation Safety rev30 enabled
- Native Vibe Compatibility rev5 enabled

Keep disabled:

- Article Reader rev17
- Resources Router rev15
- Conversion Trust Strip rev4

Do not retire patches before native parity → controlled publish → public regression.

## Astra boundary

Latest completed Astra durability commit:

`a39d07d5f032e4666f964418445371380402a49e`

Astra completed diagnosis/preservation, not native repair.

Results rev16 reference was preserved at:

`wix/rollback/results-showcase-rev16-native-recovery-reference-2026-09-12.json`

commit:

`c2e3f35de3fcf48dc4d032197333ac96c9431b50`

No later GPT-Work commit superseded that Astra completion boundary before the current checkpoint rewrite.

## Native editor access

Ryan’s authenticated local Opera browser now shows Wix Vibe **Code** access and the native file tree, including:

- ResultsPage.tsx
- PostPage.tsx
- ResourcesPage.tsx
- HomePage.tsx
- AboutPage.tsx
- BookConsultationPage.tsx
- EFSpecializedSupportPage.tsx
- SATTutoringPage.tsx
- shared Header/Footer/Router/lib directories

The editor screenshot shows `ResourcesPage.tsx` importing `RESOURCE_ARTICLES` from `@/lib/resourceArticles` and `framer-motion`.

Authentication is therefore no longer the primary blocker. The remaining problem is obtaining a safe supported read/write path to actual native source through available automation/connector tooling.

## Prompting/execution upgrade

Every future implementation prompt or direct execution unit must follow:

**recover intent → inspect current state → visible outcome → exact defect → owning layer → root cause → implementation constraints → responsive cases → regression protections → PASS/FAIL acceptance tests → implement coherent batch → verify actual result → update checkpoint → only then design the next prompt**

Do not let animation, spacing, hover effects, focus-ring changes, or unrelated technical cleanup substitute for the requested visual/content deliverable.

## Current next work unit

**P0.1 native Results ownership/parity.**

1. Get the actual current `ResultsPage.tsx` source through a supported path.
2. Back up/read it before mutation.
3. Compare section-by-section to accepted public Results + preserved rev16 reference.
4. Resolve testimonial/review ownership; avoid duplicate hard-coded systems.
5. Implement native parity only if source is safely writable.
6. Run configured build/typecheck/lint where available.
7. Inspect native preview desktop/tablet/mobile.
8. Do not publish and do not modify protected runtime patches.

If native source remains inaccessible to automation, first establish a supported source path through existing Vibe GitHub integration/export/CLI/manual Code tooling without changing production.

Then continue:

**PostPage validation → Homework/Motivation in-body visuals → portrait crop → native SEO/head verification → full responsive/accessibility sweep → controlled publish → public regression → consultation E2E → one-at-a-time patch retirement → flagship publication decision → domain/GSC owner-gated launch work → final acceptance.**

## Completion

Website is **NOT FINISHED / NOT FROZEN**.
